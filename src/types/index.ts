export type INewUser = {
  name: string;
  email: string;
  username: string;
  password: string;
};

export type Iuser = {
  id: string;
  name: string;
  username: string;
  email: string;
  imageUrl: string;
}
